import { FiltroarticulosPipe } from './filtroarticulos.pipe';

describe('FiltroarticulosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroarticulosPipe();
    expect(pipe).toBeTruthy();
  });
});
